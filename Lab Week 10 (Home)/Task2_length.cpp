#include<iostream>
#include<cmath>

using namespace std;

main()
{
    float length, width, height, output;
    cout << "Enter length, width, height (in meters): ";
    cin >> length;
    cin >> width;
    cin >> height;
}